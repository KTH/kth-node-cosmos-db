jest.fn('@azure/cosmos')

const {
  createDatabase,
  createCollection,
  increaseThroughput,
  getCollection,
  getCollectionOffer
} = require('./cosmosDb')

describe('Helper module "cosmosDb"', () => {
  it.todo('exists')
})
